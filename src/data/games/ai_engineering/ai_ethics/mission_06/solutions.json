{
  "mission_06": "import numpy as np\n\ndef toxicity_filter(text, classifier, threshold=0.7):\n    categories = ['hate', 'harassment', 'self_harm', 'sexual', 'violence']\n    scores = classifier.predict(text)\n    should_block = any(scores.get(cat, 0) > threshold for cat in categories)\n    return scores, should_block"
}
