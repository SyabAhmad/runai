{
  "title": "Actor-Critic (A2C)",
  "id": "mission_06",
  "type": "ai_code",
  "xpReward": 110,
  "difficulty": "medium",
  "initialState": {
    "content": "# RL Mission 06: Actor-Critic (A2C)\n#\n# Actor-Critic combines value-based and policy-based methods.\n# The Critic evaluates how good a state is (V(s)).\n# The Actor decides which action to take (pi(a|s)).\n#\n# Advantage: A(s,a) = Q(s,a) - V(s) ≈ r + gamma*V(s') - V(s)\n#\n# Your goal: Implement actor_critic(env, episodes, alpha, gamma)\n#   1. Initialize actor (policy) and critic (value) parameters\n#   2. For each episode:\n#      a. Run trajectory, collecting states, actions, rewards\n#      b. Compute advantages: A = r + gamma*V(s') - V(s)\n#      c. Update actor: increase prob of good actions\n#      d. Update critic: reduce value prediction error\n#   3. Return trained parameters and rewards\n\nimport numpy as np\n\ndef actor_critic(env, episodes, alpha, gamma):\n    # TODO: Initialize actor and critic parameters\n    # TODO: Collect trajectory\n    # TODO: Compute advantages\n    # TODO: Update both actor and critic\n    pass"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:def actor_critic",
      "must_include:advantage",
      "must_include:critic",
      "must_include:actor",
      "must_include:return"
    ]
  }
}
