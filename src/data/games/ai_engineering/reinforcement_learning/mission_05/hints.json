{
  "mission_05": [
    "Replay buffer: store tuples `(state, action, reward, next_state, done)` and sample `batch_size` randomly.",
    "Target: `target = reward + gamma * np.max(target_network(next_states)) * (1 - done)`.",
    "Soft update: `target_params = tau * q_params + (1 - tau) * target_params`."
  ]
}
