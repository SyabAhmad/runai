{
  "mission_06": "Cross-validation gives a more robust estimate of model performance than a single train-test split. Implement 5-fold cross-validation manually: split data into 5 folds, train on 4 folds and validate on the remaining fold, rotating through all combinations. Return the mean accuracy across all folds."
}
