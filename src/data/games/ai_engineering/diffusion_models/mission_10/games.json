{
  "title": "Image Inpainting & Editing",
  "id": "mission_10",
  "type": "ai_code",
  "xpReward": 120,
  "difficulty": "hard",
  "initialState": {
    "content": "# Diffusion Models Mission 10: Image Inpainting & Editing\n#\n# Inpainting fills masked regions of an image using diffusion.\n# The key idea: constrain the diffusion process to keep unmasked regions fixed.\n#\n# Algorithm:\n# 1. Add noise to original image (forward process)\n# 2. At each reverse step, replace unmasked region with noisy original\n# 3. Only the masked region is generated by the model\n#\n# x_t = mask * x_t_generated + (1 - mask) * x_t_original_noisy\n#\n# Your goal: Implement inpainting(original_image, mask, model, prompt, steps)\n#   1. Encode original image to latent\n#   2. Add noise to latent at each timestep\n#   3. Run reverse diffusion, blending masked/unmasked regions\n#   4. Decode final latent to image\n\nimport numpy as np\n\ndef inpainting(original_image, mask, model, prompt, steps=50):\n    # TODO: Encode original to latent\n    # TODO: Create masked noisy latent\n    # TODO: Run reverse diffusion with blending\n    # TODO: Decode and return\n    pass"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:def inpainting",
      "must_include:mask",
      "must_include:blend",
      "must_include:original",
      "must_include:return"
    ]
  }
}
