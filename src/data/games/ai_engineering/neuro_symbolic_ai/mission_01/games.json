{
  "title": "Implement Logic Neural Networks",
  "id": "mission_01",
  "type": "ai_code",
  "xpReward": 60,
  "difficulty": "easy",
  "initialState": {
    "content": "import numpy as np\n\n\nclass LogicNeuron:\n    \"\"\"A neuron that implements logical operations using continuous relaxation.\"\"\"\n\n    def __init__(self, n_inputs, logic_type='and'):\n        self.logic_type = logic_type\n        # TODO: Initialize weights for logical AND/OR operations\n        # For AND: all weights positive; for OR: at least one large weight\n        if logic_type == 'and':\n            self.weights = np.ones(n_inputs) * 10.0\n        elif logic_type == 'or':\n            self.weights = np.ones(n_inputs) * ???\n        else:\n            self.weights = np.random.randn(n_inputs)\n        self.bias = -np.sum(self.weights) / 2\n\n    def _sigmoid(self, x):\n        \"\"\"Numerically stable sigmoid.\"\"\"\n        return np.where(x >= 0,\n                       1 / (1 + np.exp(-x)),\n                       np.exp(x) / (1 + np.exp(x)))\n\n    def forward(self, x):\n        \"\"\"Compute logical operation output (continuous relaxation).\"\"\"\n        # TODO: Compute weighted sum + bias\n        z = ???\n\n        if self.logic_type == 'and':\n            # AND: output high only when ALL inputs are high\n            # Use product approximation: sigmoid(sum(w*x) + bias)\n            return self._sigmoid(z)\n        elif self.logic_type == 'or':\n            # OR: output high when ANY input is high\n            # Use max approximation\n            return self._sigmoid(z)\n        elif self.logic_type == 'not':\n            # NOT: invert the single input\n            return self._sigmoid(???)\n        return self._sigmoid(z)\n\n\nclass LogicLayer:\n    \"\"\"A layer of logic neurons implementing a logical formula.\"\"\"\n\n    def __init__(self, n_inputs, n_neurons, logic_types=None):\n        self.neurons = []\n        if logic_types is None:\n            logic_types = ['and'] * n_neurons\n\n        for i in range(n_neurons):\n            # TODO: Create logic neurons with specified types\n            neuron = ???\n            self.neurons.append(neuron)\n\n    def forward(self, x):\n        \"\"\"Forward pass through all logic neurons.\"\"\"\n        # TODO: Compute output of each neuron\n        outputs = []\n        for neuron in self.neurons:\n            outputs.append(???)\n        return np.array(outputs)\n\n\ndef logic_to_network(x1, x2, formula='x1 AND x2'):\n    \"\"\"Build a logic neural network for a given formula.\"\"\"\n    x = np.array([x1, x2])\n\n    if 'AND' in formula:\n        neuron = LogicNeuron(2, 'and')\n    elif 'OR' in formula:\n        neuron = LogicNeuron(2, 'or')\n    else:\n        neuron = LogicNeuron(2, 'and')\n\n    return neuron.forward(x)\n\n\nif __name__ == \"__main__\":\n    np.random.seed(42)\n\n    # Test AND gate\n    and_neuron = LogicNeuron(2, 'and')\n    print(\"AND gate:\")\n    for x1 in [0, 0, 1, 1]:\n        for x2 in [0, 1, 0, 1]:\n            out = logic_to_network(x1, x2, 'x1 AND x2')\n            print(f\"  ({x1}, {x2}) -> {out:.4f}\")\n\n    # Test OR gate\n    or_neuron = LogicNeuron(2, 'or')\n    print(\"\\nOR gate:\")\n    for x1 in [0, 0, 1, 1]:\n        for x2 in [0, 1, 0, 1]:\n            x = np.array([x1, x2])\n            out = or_neuron.forward(x)\n            print(f\"  ({x1}, {x2}) -> {out:.4f}\")\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:LogicNeuron",
      "must_include:LogicLayer",
      "must_include:_sigmoid",
      "must_include:np.exp",
      "must_include:logic_type",
      "must_include:forward"
    ]
  }
}
