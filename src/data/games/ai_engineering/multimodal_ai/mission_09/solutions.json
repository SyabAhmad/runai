{
  "mission_09": "import numpy as np\n\ndef text_to_image(prompt, clip_encoder, diffusion_model, steps=50):\n    text_embed = clip_encoder(prompt)\n    null_embed = np.zeros_like(text_embed)\n    z = np.random.randn(1, 4, 64, 64)\n    T = 1000\n    steps_list = np.linspace(0, T - 1, steps, dtype=int)[::-1]\n    for i, t in enumerate(steps_list):\n        eps_cond = diffusion_model(z, t, text_embed)\n        eps_uncond = diffusion_model(z, t, null_embed)\n        eps = eps_uncond + 7.5 * (eps_cond - eps_uncond)\n        ab = alpha_bar[t]\n        ab_prev = alpha_bar[steps_list[i + 1]] if i + 1 < len(steps_list) else alpha_bar[0]\n        z = np.sqrt(ab_prev) * (z - np.sqrt(1 - ab) * eps) / np.sqrt(ab) + np.sqrt(1 - ab_prev) * eps\n    return vae_decoder(z)"
}
