{
  "mission_08": [
    "First write a helper function that routes a single sub-task to the right specialist based on its prefix.",
    "Check if the task contains \" and \" to detect combined tasks, then split and handle each part.",
    "For each sub-task, strip the prefix before passing to the specialist: `sub.split(\":\", 1)[1].strip()`."
  ]
}
