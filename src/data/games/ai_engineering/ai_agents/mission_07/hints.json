{
  "mission_07": [
    "Wrap `risky_calculator()` in try/except ValueError and except ZeroDivisionError blocks.",
    "For `fetch_user_data()`, check if result is None and return a \"User not found\" message with status \"error\".",
    "Use the startswith() pattern to route between \"calc:\" and \"user:\" commands."
  ]
}
