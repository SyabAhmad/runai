{
  "mission_02": "def calculator(expression):\n    return eval(expression)\n\ndef agent(task):\n    if task.startswith(\"calc:\"):\n        expr = task[5:]\n        result = calculator(expr)\n    else:\n        result = \"I cannot do that yet\"\n    return {\"task\": task, \"result\": result}"
}
