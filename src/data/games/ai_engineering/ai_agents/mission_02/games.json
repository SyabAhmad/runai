{
  "title": "Agent with a Single Tool",
  "id": "mission_02",
  "type": "ai_code",
  "xpReward": 70,
  "difficulty": "easy",
  "initialState": {
    "content": "# Agent Mission 02: Agent with a Single Tool\n#\n# Agents become powerful when they can use tools.\n# A tool is a function the agent can call to perform actions.\n#\n# We have a calculator tool:\n#   def calculator(expression):\n#       return eval(expression)\n#\n# Your goal: Complete the agent(task) function so that:\n#   1. If task starts with \"calc:\", extract the expression\n#      and call calculator(expr) to get the result\n#   2. Otherwise, return {\"task\": task, \"result\": \"I cannot do that yet\"}\n#   3. Always return {\"task\": task, \"result\": result}\n\ndef calculator(expression):\n    return eval(expression)\n\ndef agent(task):\n    # TODO: check if task starts with \"calc:\"\n    #   If yes: extract the expression after \"calc:\",\n    #           call calculator(), store the result\n    #   If no: result = \"I cannot do that yet\"\n    #\n    # Return {\"task\": task, \"result\": result}\n    pass"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:def agent",
      "must_include:calculator",
      "must_include:startswith",
      "must_include:return",
      "must_include:result"
    ]
  }
}
