{
  "mission_04": [
    "Use `task.startswith(\"calc:\")`, `task.startswith(\"weather:\")`, `task.startswith(\"time:\")` to detect intent.",
    "Extract the argument with `task.split(\":\", 1)[1].strip()` to get everything after the colon.",
    "Chain if/elif/else to route to calculator(), get_weather(), or get_time() based on the prefix."
  ]
}
