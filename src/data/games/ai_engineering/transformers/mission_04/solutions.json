{
  "mission_04": "import numpy as np\n\ndef layer_norm(x, eps=1e-5):\n    mean = np.mean(x, axis=-1, keepdims=True)\n    std = np.std(x, axis=-1, keepdims=True)\n    return (x - mean) / (std + eps)\n\ndef multi_head_attention(x, W_q, W_k, W_v, W_o, num_heads):\n    Q = np.matmul(x, W_q)\n    K = np.matmul(x, W_k)\n    V = np.matmul(x, W_v)\n    d_k = K.shape[-1] // num_heads\n    heads = []\n    for i in range(num_heads):\n        q_h = Q[:, i*d_k:(i+1)*d_k]\n        k_h = K[:, i*d_k:(i+1)*d_k]\n        v_h = V[:, i*d_k:(i+1)*d_k]\n        d_k_h = k_h.shape[-1]\n        scores = np.matmul(q_h, k_h.T) / np.sqrt(d_k_h)\n        e_x = np.exp(scores - np.max(scores, axis=-1, keepdims=True))\n        attn = e_x / np.sum(e_x, axis=-1, keepdims=True)\n        heads.append(np.matmul(attn, v_h))\n    concat = np.concatenate(heads, axis=-1)\n    return np.matmul(concat, W_o)\n\ndef encoder_block(x, W_q, W_k, W_v, W_o, W_ff1, W_ff2, num_heads):\n    attn = multi_head_attention(x, W_q, W_k, W_v, W_o, num_heads)\n    x1 = layer_norm(x + attn)\n    ffn = np.matmul(np.maximum(np.matmul(x1, W_ff1), 0), W_ff2)\n    return layer_norm(x1 + ffn)"
}
