{
  "mission_05": "You built a decoder block! The causal mask ensures autoregressive generation — the model can only use information from tokens it has already produced. Cross-attention lets the decoder \"read\" the encoder's understanding while generating. This is the architecture behind machine translation, text summarization, and all generative transformers."
}
