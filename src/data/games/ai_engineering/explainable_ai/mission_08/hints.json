{
  "mission_08": [
    "Initialize W_k and W_v same as W_q: np.random.randn(d_model, d_model) * np.sqrt(2.0 / d_model).",
    "Split heads: x.reshape(batch_size, -1, self.n_heads, self.d_k).transpose(0, 2, 1, 3).",
    "Scale scores by sqrt(d_k): scores / np.sqrt(self.d_k).",
    "Softmax: exp_scores = np.exp(scores - np.max(scores, axis=-1, keepdims=True)); attention_weights = exp_scores / np.sum(exp_scores, axis=-1, keepdims=True).",
    "Apply attention: output = attention_weights @ V."
  ]
}
