{
  "title": "Cron Jobs",
  "initialState": {
    "content": "#!/bin/bash\n# Create a cron job entry that runs a backup script every day at 2 AM\n# Format: minute hour day month weekday command\n# Write the cron entry to stdout (don't actually install it)\n\n"
  },
  "validation": {
    "type": "terminal",
    "rules": [
      "must_include:0",
      "must_include:2",
      "must_include:*",
      "must_include:cron"
    ]
  },
  "id": "mission_08",
  "type": "terminal",
  "xpReward": 200
}