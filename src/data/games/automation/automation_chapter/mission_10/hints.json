{
  "mission_10": [
    "Use date +%Y%m%d_%H%M%S to create a timestamp",
    "Check if source directory exists with -d flag",
    "rsync -av is a good choice for backups",
    "Log to a file with >> backup.log"
  ]
}