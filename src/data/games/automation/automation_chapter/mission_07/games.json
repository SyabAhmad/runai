{
  "title": "Exit Codes",
  "initialState": {
    "content": "#!/bin/bash\n# Run a command and check if it succeeded\n# Use $? to check exit code (0 = success)\n\n"
  },
  "validation": {
    "type": "terminal",
    "rules": [
      "must_include:$?",
      "must_include:if",
      "must_include:= 0"
    ]
  },
  "id": "mission_07",
  "type": "terminal",
  "xpReward": 150
}