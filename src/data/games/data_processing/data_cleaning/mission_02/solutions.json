{
    "mission_02":  "def dedupe_and_normalize(df):\\n    df = df.drop_duplicates()\\n\\n    if \"name\" in df.columns:\\n        df[\"name\"] = df[\"name\"].astype(str).str.strip()\\n\\n    if \"city\" in df.columns:\\n        df[\"city\"] = df[\"city\"].astype(str).str.strip().str.lower()\\n\\n    return df"
}
