{
    "mission_02":  [
                       "drop_duplicates() removes repeated records.",
                       "Use .str.strip() for leading/trailing spaces.",
                       "Use .str.lower() for case normalization."
                   ]
}
