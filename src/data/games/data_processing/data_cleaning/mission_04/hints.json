{
  "mission_04": [
    "Use df['column'].fillna(df['column'].median()) for numeric imputation",
    "Use df['column'].fillna(df['column'].mode()[0]) for categorical imputation",
    "Check missing values with df.isnull().sum() before and after filling"
  ]
}
