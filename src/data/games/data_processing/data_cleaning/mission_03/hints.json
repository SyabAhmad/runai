{
    "mission_03":  [
                       "Use select_dtypes to detect object/category columns.",
                       "pd.get_dummies can encode multiple columns at once.",
                       "drop_first=True reduces multicollinearity."
                   ]
}
