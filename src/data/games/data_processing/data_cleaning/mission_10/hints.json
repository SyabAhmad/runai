{
  "mission_10": [
    "Chain operations: drop_duplicates first, then text cleaning, then type conversion",
    "Use pd.to_numeric with errors='coerce' for safe numeric conversion",
    "Use pd.to_datetime with multiple format attempts in a try/except loop",
    "Remove currency symbols with str.replace('$', '').str.replace(',', '')"
  ]
}
