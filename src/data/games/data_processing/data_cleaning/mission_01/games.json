{
  "title": "Remove Duplicate Rows",
  "id": "mission_01",
  "type": "ai_code",
  "xpReward": 60,
  "difficulty": "easy",
  "initialState": {
    "content": "import pandas as pd\n\n# TODO: Create a DataFrame with duplicate rows\ndata = {\n    'name': ['Alice', 'Bob', 'Alice', 'Charlie', 'Bob'],\n    'age': [25, 30, 25, 35, 30],\n    'city': ['NY', 'LA', 'NY', 'Chicago', 'LA']\n}\ndf = pd.DataFrame(data)\n\n# TODO: Remove duplicate rows using drop_duplicates()\ndf_clean = ...\n\n# TODO: Verify no duplicates remain\nprint(f\"Original rows: {len(df)}\")\nprint(f\"Clean rows: {len(df_clean)}\")\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:drop_duplicates",
      "must_include:pandas",
      "must_include:DataFrame"
    ]
  }
}
