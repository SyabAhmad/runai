{
  "mission_01": [
    "Use df.drop_duplicates() to remove rows where all columns have identical values",
    "The 'keep' parameter controls which duplicate to retain: 'first', 'last', or False",
    "Check for duplicates with df.duplicated().sum() before and after cleaning"
  ]
}
