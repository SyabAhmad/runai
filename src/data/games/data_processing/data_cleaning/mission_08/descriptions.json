{
    "mission_08":  "Write build_preprocessor(numeric_cols, categorical_cols) that creates a ColumnTransformer using StandardScaler for numeric and OneHotEncoder(handle_unknown=\"ignore\") for categorical columns."
}
