{
  "mission_08": [
    "Create normalized columns with df['col'].str.lower() for case-insensitive comparison",
    "Sort by the priority column with df.sort_values('score', ascending=False)",
    "Drop duplicates on normalized columns with keep='first' to keep the highest score"
  ]
}
