{
  "mission_07": [
    "Use df.describe() for summary statistics of numeric columns",
    "Use df.isnull().sum() to count missing values per column",
    "Access column statistics with df[col].mean(), df[col].std(), df[col].min(), df[col].max()"
  ]
}
