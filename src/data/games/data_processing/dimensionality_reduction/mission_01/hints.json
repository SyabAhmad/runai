{"mission_01": ["Always standardize data before PCA: scaler = StandardScaler(); X_scaled = scaler.fit_transform(X).", "Create pca = PCA(n_components=2) and use X_pca = pca.fit_transform(X_scaled).", "Access pca.explained_variance_ratio_ for per-component variance and np.cumsum() for cumulative."]}
