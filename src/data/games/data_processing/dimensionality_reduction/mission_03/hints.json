{"mission_03": ["Import umap and create reducer = umap.UMAP(n_components=2, random_state=42).", "Apply with X_umap = reducer.fit_transform(X) and combine with labels in a DataFrame.", "UMAP is faster than t-SNE and preserves both local and global structure, making it suitable for larger datasets."]}
