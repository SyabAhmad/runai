{
  "title": "Recursive Feature Elimination",
  "id": "mission_07",
  "type": "ai_code",
  "xpReward": 90,
  "difficulty": "medium",
  "initialState": {
    "content": "import pandas as pd\nimport numpy as np\nfrom sklearn.feature_selection import RFE\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.datasets import make_classification\n\n# TODO: Create a classification dataset\nX, y = make_classification(n_samples=100, n_features=10, n_informative=5, random_state=42)\n\n# TODO: Apply RFE to select top 5 features\nmodel = LogisticRegression(max_iter=1000)\nrfe = RFE(estimator=model, n_features_to_select=5)\nX_selected = ...\n\nprint(f\"Original shape: {X.shape}\")\nprint(f\"Selected shape: {X_selected.shape}\")\nprint(f\"Selected features: {rfe.support_}\")\nprint(f\"Feature rankings: {rfe.ranking_}\")\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:RFE",
      "must_include:n_features_to_select",
      "must_include:support_"
    ]
  }
}
