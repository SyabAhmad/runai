{
    "mission_01":  "import numpy as np\\n\\ndef add_interaction_features(df):\\n    safe_visits = df[\"visits\"].replace(0, np.nan)\\n    df[\"total_spend_per_visit\"] = (df[\"total_spend\"] / safe_visits).fillna(0)\\n    df[\"spend_times_items\"] = df[\"total_spend\"] * df[\"items_count\"]\\n    return df"
}
