{
  "title": "Color and Channel Augmentation",
  "id": "mission_03",
  "type": "ai_code",
  "xpReward": 70,
  "difficulty": "easy",
  "initialState": {
    "content": "import numpy as np\nimport albumentations as A\n\n# TODO: Create a color augmentation pipeline\n# Add HueSaturationValue with hue_shift_limit=20, sat_shift_limit=30, val_shift_limit=20\n# Add RGBShift with r_shift_limit=20, g_shift_limit=20, b_shift_limit=20\n# Add CLAHE with clip_limit=2.0, tile_grid_size=(8, 8)\n# Add ChannelShuffle with probability 0.2\n\ndef color_augmentation_pipeline():\n    # TODO: Build and return the color augmentation pipeline\n    pass\n\n# TODO: Apply the pipeline to a sample image and verify output\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:HueSaturationValue",
      "must_include:RGBShift",
      "must_include:CLAHE",
      "must_include:ChannelShuffle",
      "must_include:hue_shift_limit",
      "must_include:sat_shift_limit",
      "must_include:val_shift_limit",
      "must_include:r_shift_limit",
      "must_include:clip_limit",
      "must_include:tile_grid_size"
    ]
  }
}
