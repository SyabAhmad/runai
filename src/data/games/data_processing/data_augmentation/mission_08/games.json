{
  "title": "Data Augmentation Pipeline",
  "id": "mission_08",
  "type": "ai_code",
  "xpReward": 100,
  "difficulty": "hard",
  "initialState": {
    "content": "import numpy as np\nimport pandas as pd\nfrom sklearn.datasets import make_classification\nfrom imblearn.over_sampling import SMOTE\n\n# Create imbalanced dataset\nX, y = make_classification(n_samples=300, n_features=6, n_informative=4,\n                           weights=[0.85, 0.15], random_state=42)\n\n# TODO: Apply SMOTE to balance classes\n# TODO: Apply mixup on the SMOTE-resampled data to further augment\n# TODO: Create a function that chains SMOTE + mixup\n# TODO: Track dataset size at each stage: original -> SMOTE -> mixup\n# TODO: Print the augmentation pipeline summary\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:SMOTE",
      "must_include:mixup",
      "must_include:fit_resample"
    ]
  }
}
