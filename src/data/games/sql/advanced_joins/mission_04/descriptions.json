{
  "mission_04": "A CROSS JOIN produces the Cartesian product of two tables, pairing every row from the first table with every row from the second. This is useful for generating all possible combinations, such as product variants, schedule grids, or test data. Unlike other joins, a CROSS JOIN has no ON clause and simply multiplies the row counts together."
}
