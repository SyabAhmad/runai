{
  "mission_08": "LATERAL joins enable correlated subqueries in the FROM clause, allowing you to reference columns from preceding tables. This is particularly powerful for 'top N per group' queries where you need to run a limited subquery for each row in the outer table. LATERAL joins are more efficient than window function approaches for certain top-N scenarios."
}
