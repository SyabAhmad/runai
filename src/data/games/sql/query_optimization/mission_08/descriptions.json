{
  "mission_08": "Applying functions to indexed columns prevents the database from using the index, forcing a sequential scan. The solution is to rewrite the query so the indexed column stands alone on one side of the comparison. Instead of DATE(created_at) = '2024-03-15', use a range: created_at >= '2024-03-15' AND created_at < '2024-03-16'."
}
