{
  "mission_08": "Excellent query rewriting! You've learned a critical optimization principle: never apply functions to indexed columns in WHERE clauses. By restructuring the condition to use range comparisons, you enable the database to leverage the index. This single pattern can improve query performance by orders of magnitude on large tables."
}
