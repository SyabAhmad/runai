{
  "mission_03": "The marketing team needs a list of customers who have placed at least one order — these are the customers eligible for a loyalty campaign. A colleague tried solving this with a `JOIN`, but the result has duplicate customers (one row per order). You need to rewrite it using `EXISTS` so each customer appears exactly once. Return `customer_id` and `name` for every customer who has at least one order in the `orders` table."
}