{
    "mission_05":  [
                       "Filter in CTE before SUM/GROUP BY.",
                       "Use only needed columns in CTE.",
                       "Alias SUM(total_amount) as total_spend."
                   ]
}
