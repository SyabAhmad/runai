{
  "mission_09": "The ops manager needs a single-row 2025 summary: total orders, completed orders, and cancelled orders. Instead of three separate queries, write a single query that computes all three metrics in one pass using conditional aggregation with `CASE WHEN` inside `SUM`. Filter to the full year 2025 using a date range, then count each status type conditionally."
}