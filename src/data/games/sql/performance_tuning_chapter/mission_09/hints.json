{
    "mission_09":  [
                       "Use conditional SUM for each metric.",
                       "Keep all metrics in one SELECT.",
                       "Filter date range once in WHERE."
                   ]
}
