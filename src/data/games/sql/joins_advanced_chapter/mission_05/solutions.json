{
    "mission_05":  "SELECT o.order_id, SUM(oi.quantity * p.price) AS order_total FROM orders o INNER JOIN order_items oi ON o.order_id = oi.order_id INNER JOIN products p ON oi.product_id = p.product_id GROUP BY o.order_id ORDER BY order_total DESC;"
}
