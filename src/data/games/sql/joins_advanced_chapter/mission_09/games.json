{
    "title":  "SQL Mission 9: Join With Aggregated Subquery",
    "initialState":  {
                         "content":  "SELECT c.name, o.total_orders\\nFROM customers c;"
                     },
    "validation":  {
                       "type":  "sql",
                       "rules":  [
                                     "must_include:left join (select customer_id, count(*) as total_orders from orders group by customer_id) o",
                                     "must_include:on c.customer_id = o.customer_id",
                                     "must_include:coalesce(o.total_orders, 0) as total_orders",
                                     "must_include:order by total_orders desc"
                                 ]
                   },
    "id":  "mission_09",
    "type":  "sql_debug"
}
