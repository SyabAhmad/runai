{
  "mission_02": [
    "Define the parameter as p_customer_id INTEGER.",
    "Define RETURNS TABLE with columns matching the orders table.",
    "Use RETURN QUERY SELECT * FROM orders WHERE customer_id = p_customer_id."
  ]
}
