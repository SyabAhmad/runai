
{
    "mission_01":  [
                       "Start by selecting only the name column.",
                       "Add both filters in WHERE using AND.",
                       "Use ORDER BY salary DESC for ranking."
                   ]
}
