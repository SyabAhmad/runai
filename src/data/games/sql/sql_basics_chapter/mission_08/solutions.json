{
    "mission_08":  "WITH dept_payroll AS (SELECT department, SUM(salary) AS total_payroll FROM employees GROUP BY department) SELECT department, total_payroll FROM dept_payroll WHERE total_payroll \u003e 300000 ORDER BY total_payroll DESC;"
}
