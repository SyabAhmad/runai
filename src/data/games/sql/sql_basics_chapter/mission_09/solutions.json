{
  "mission_09": "WITH ranked AS (SELECT department, name, salary, ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) AS rn FROM employees) SELECT department, name, salary, rn FROM ranked WHERE rn \u003c= 3 ORDER BY department, salary DESC;"
}
