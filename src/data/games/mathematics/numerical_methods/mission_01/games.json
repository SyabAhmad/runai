{
  "title": "Bisection Method",
  "id": "mission_01",
  "type": "ai_code",
  "xpReward": 60,
  "difficulty": "easy",
  "initialState": {
    "content": "import numpy as np\n\ndef bisection_method(f, a, b, tol=1e-10, max_iter=100):\n    \"\"\"\n    Find root of f(x) = 0 using bisection method\n    Requires f(a) * f(b) < 0 (sign change in interval)\n    f: function to find root of\n    a, b: interval endpoints\n    tol: tolerance for convergence\n    max_iter: maximum iterations\n    \"\"\"\n    # TODO: Verify sign change\n    if f(a) * f(b) >= 0:\n        raise ValueError(\"f(a) and f(b) must have opposite signs\")\n    \n    history = []\n    \n    for i in range(max_iter):\n        # TODO: Compute midpoint\n        c = ...\n        \n        history.append(c)\n        \n        # TODO: Check if root found or interval small enough\n        if f(c) == 0 or (b - a) / 2 < tol:\n            return c, history\n        \n        # TODO: Update interval based on sign of f(c)\n        if ...:\n            a = c\n        else:\n            b = c\n    \n    return (a + b) / 2, history\n\ndef estimate_iterations(a, b, tol):\n    \"\"\"\n    Estimate number of iterations needed for bisection\n    n >= log2((b-a)/tol)\n    \"\"\"\n    # TODO: Compute estimated iterations\n    n = ...\n    return int(np.ceil(n))\n\n# Test: find root of x^3 - x - 2 = 0 in [1, 2]\ndef f(x):\n    return x**3 - x - 2\n\nroot, history = bisection_method(f, 1.0, 2.0)\nprint(f\"Root: {root:.10f}\")\nprint(f\"f(root): {f(root):.2e}\")\nprint(f\"Iterations: {len(history)}\")\nprint(f\"Estimated iterations: {estimate_iterations(1, 2, 1e-10)}\")"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:bisection",
      "must_include:midpoint",
      "must_include:numpy"
    ]
  }
}
