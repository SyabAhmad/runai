{
  "title": "Trapezoidal Integration",
  "id": "mission_03",
  "type": "ai_code",
  "xpReward": 70,
  "difficulty": "easy",
  "initialState": {
    "content": "import numpy as np\n\ndef trapezoidal_rule(f, a, b, n=100):\n    \"\"\"\n    Numerical integration using trapezoidal rule\n    Integral ≈ h/2 * [f(a) + 2*sum(f(x_i)) + f(b)]\n    f: function to integrate\n    a, b: integration limits\n    n: number of subintervals\n    \"\"\"\n    # TODO: Compute step size\n    h = ...\n    \n    # TODO: Create grid points\n    x = ...\n    \n    # TODO: Evaluate function at grid points\n    y = ...\n    \n    # TODO: Apply trapezoidal rule\n    integral = ...\n    \n    return integral\n\ndef adaptive_trapezoidal(f, a, b, tol=1e-8, max_depth=20):\n    \"\"\"\n    Adaptive trapezoidal rule that refines until tolerance is met\n    \"\"\"\n    def recurse(f, a, b, tol, depth):\n        # TODO: Compute with 1 and 2 panels\n        one_panel = trapezoidal_rule(f, a, b, 1)\n        two_panels = trapezoidal_rule(f, a, b, 2)\n        \n        # TODO: Estimate error\n        error = ...\n        \n        if abs(error) < tol or depth >= max_depth:\n            return two_panels\n        \n        # TODO: Recursively refine each half\n        mid = (a + b) / 2\n        return recurse(f, a, mid, tol/2, depth+1) + recurse(f, mid, b, tol/2, depth+1)\n    \n    return recurse(f, a, b, tol, 0)\n\ndef trapezoidal_error_bound(f_double_prime, a, b, n):\n    \"\"\"\n    Error bound for trapezoidal rule: |E| <= (b-a)^3/(12*n^2) * max|f''(x)|\n    \"\"\"\n    # TODO: Compute error bound\n    error_bound = ...\n    return error_bound\n\n# Test: integrate sin(x) from 0 to pi (exact = 2)\nresult = trapezoidal_rule(np.sin, 0, np.pi, n=100)\nprint(f\"Trapezoidal: {result:.10f}\")\nprint(f\"Exact: 2.0\")\nprint(f\"Error: {abs(result - 2):.2e}\")"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:trapezoidal",
      "must_include:integration",
      "must_include:numpy"
    ]
  }
}
