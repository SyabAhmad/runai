{
  "title": "Momentum Gradient Descent",
  "id": "mission_02",
  "type": "ai_code",
  "xpReward": 65,
  "difficulty": "easy",
  "initialState": {
    "content": "import numpy as np\n\ndef momentum_gradient_descent(f, grad_f, x0, learning_rate=0.01, momentum=0.9, max_iter=1000, tol=1e-6):\n    \"\"\"\n    Implement gradient descent with momentum\n    momentum: coefficient for velocity accumulation (typically 0.9)\n    \"\"\"\n    x = x0.copy()\n    velocity = np.zeros_like(x)\n    history = [x.copy()]\n    \n    for i in range(max_iter):\n        grad = grad_f(x)\n        \n        # TODO: Update velocity with momentum\n        velocity = ...\n        \n        # TODO: Update position using velocity\n        x = ...\n        \n        history.append(x.copy())\n        \n        if np.linalg.norm(grad) < tol:\n            break\n    \n    return x, history\n\n# Test with Rosenbrock-like function\ndef f(x):\n    return x[0]**2 + 2*x[1]**2\n\ndef grad_f(x):\n    return np.array([2*x[0], 4*x[1]])\n\nx0 = np.array([3.0, 3.0])\nx_opt, history = momentum_gradient_descent(f, grad_f, x0, learning_rate=0.1, momentum=0.9)\nprint(f\"Optimal x: {x_opt}\")"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:velocity",
      "must_include:momentum",
      "must_include:numpy"
    ]
  }
}
