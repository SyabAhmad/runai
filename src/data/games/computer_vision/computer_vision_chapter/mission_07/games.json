{
  "title": "Inference",
  "initialState": {
    "content": "import tensorflow as tf\nimport cv2\n\nmodel = tf.keras.applications.MobileNetV2(weights=\"imagenet\")\nimg = cv2.imread(\"image.jpg\")\n# TODO: Run inference on the image\n# Preprocess with tf.keras.applications.mobilenet_v2.preprocess_input()\n"
  },
  "validation": {
    "type": "ai_code",
    "rules": [
      "must_include:preprocess_input",
      "must_include:predict",
      "must_include:resize"
    ]
  },
  "id": "mission_07",
  "type": "ai_code",
  "xpReward": 150
}