{
  "mission_08": [
    "VM Scale Sets (VMSS) let you deploy identical VMs that scale together",
    "--instance-count sets initial size",
    "You can configure auto-scale rules based on metrics"
  ]
}